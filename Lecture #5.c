#include<stdio.h>
//write a program to check wether a given number is positive or non positive
main()
{
	int a;
	printf("Enter a number : ");
	scanf("%d",&a);
	if(a>0)
	{
		printf("positive");
	}
	else{
		printf("Non positive");
	}
}
